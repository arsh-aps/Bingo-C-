Assignment-2 self evaluation

Our programs:
    1. Are free of runtime errors
    2. Generates the expected output (able to play the game)
    3. Implement a list ADT as required
    4. Pass testing with both good and bad input files
    5. Performs checking of command line arguments
    6. Performs checking of CardFile for correct format
    7. Compile correctly and do not display warnings
    8. Use a makefile as required (our programs compile with $make -f A2Makefile)

We invested approximately 40 hours working on this assignment.